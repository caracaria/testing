name: Automated VPS & 3x-ui Panel

on:
  workflow_dispatch:

jobs:
  run-vps:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup & Connect Tailscale
        uses: tailscale/github-action@v2
        with:
          authkey: ${{ secrets.TAILSCALE_AUTHKEY }}

      - name: Install & Setup ngrok
        run: |
          curl -sSL https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null
          echo "deb https://ngrok-agent.s3.amazonaws.com buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list
          sudo apt update && sudo apt install ngrok -y
          ngrok config add-authtoken ${{ secrets.NGROK_AUTHTOKEN }}

      - name: Install 3x-ui Panel
        run: |
          curl -Ls https://raw.githubusercontent.com/mhsanaei/3x-ui/master/install.sh -o install.sh
          sudo bash install.sh <<EOF
          y
          admin
          admin1234
          2053
          
          4
          y
          EOF

      - name: Start ngrok Tunnel & Keep Alive
        run: |
          nohup ngrok http 2053 > /dev/null 2>&1 &
          sleep 6
          
          echo "=========================================="
          echo "Public ngrok URL:"
          curl -s http://127.0.0.1:4040/api/tunnels | grep -o 'https://[^"]*' || true
          echo ""
          echo "Panel Path & Settings:"
          sudo x-ui settings | grep -E "port|webBasePath|Access URL" || true
          echo "=========================================="
          
          sleep 6h
