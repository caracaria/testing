name: Manual StormDNS Install
on: [workflow_dispatch]

jobs:
  manual-install:
    runs-on: ubuntu-latest
    steps:
      - name: Open Terminal (tmate)
        uses: mxschmitt/action-tmate@v3
        with:
          limit-access-to-actor: true
